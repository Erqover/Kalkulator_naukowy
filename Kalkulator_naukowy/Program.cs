namespace armcrode
{
    class Kalkulator
    {
        static void Main(string[] args)
        {
            bool program = true;
            int case_switch = 0;
            string temp__case_switch ="";
            while (program)
            {
                if (temp__case_switch == null || temp__case_switch == "")
                {
                    temp__case_switch = Console.ReadLine();
                }

                switch(case_switch)
                { 
                    default:
                        Console.WriteLine("nie podałeś żadnej opcji");
                    break;

            }
            }
        }
    }
}